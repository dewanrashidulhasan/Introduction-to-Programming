#include<stdio.h>
#include<string.h>
int main()
{   char s[1000], t[1000];
    scanf("%s %s", &s, &t);
    int length = strlen(s);
    int lengths = strlen(t);
    for (int i = 0; i < length; i++)
    {
        
    }
     for (int i = 0; i < lengths; i++)
    {
        
    }
    printf("%d %d\n",length,lengths);
    printf("%s ",s);
    printf("%s",t);
    return 0;
}